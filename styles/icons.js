import { MaterialIcons } from '@expo/vector-icons';
import { Foundation } from '@expo/vector-icons';

export const HomeIcon = ({color, size}) =><MaterialIcons name="supervised-user-circle" size={size} color={color} />
export const FaqIcon = ({color, size}) =><Foundation name="comments" size={size} color={color} />